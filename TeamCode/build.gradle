apply plugin: 'com.android.application'

//
// build.gradle in TeamCode
//
// Most of the definitions for building your module reside in a common, shared
// file 'build.common.gradle'. Being factored in this way makes it easier to
// integrate updates to the FTC into your code. If you really need to customize
// the build definitions, you can place those customizations in this file, but
// please think carefully as to whether such customizations are really necessary
// before doing so.

// Custom definitions may go here

// Include common definitions from above.
apply from: '../build.common.gradle'
apply from: '../build.dependencies.gradle'

android {
    namespace = 'org.firstinspires.ftc.teamcode'

    packagingOptions {
        jniLibs.useLegacyPackaging true
    }

    // Local unit tests (TeamCode/src/test/java) run on the laptop JVM, no robot needed.
    // Android framework calls return defaults instead of throwing, so Common classes that only
    // touch FTC interfaces (Telemetry, HardwareMap) can be tested with fakes.
    testOptions {
        unitTests.returnDefaultValues = true
    }
}

dependencies {
    implementation project(':FtcRobotController')

    // Koala-Log 1.5.7 (2026-06-05): writes AdvantageScope-compatible .wpilog files on the Control
    // Hub. common/LogUtil wraps it; doc/LOGGING_ADVANTAGESCOPE.md says how to pull and view a log.
    // The annotation processor generates *AutoLogged subclasses for @AutoLog classes (none yet).
    // Koala-Log posts values to FTC Dashboard's telemetry and its POM does not pull Dashboard in,
    // so Dashboard is added here by hand (its page is http://192.168.43.1:8080 when the robot is on).
    // Repositories for these are in build.dependencies.gradle.
    implementation 'com.github.ori-coval.Koala-Log:KoalaLogger:1.5.7'
    annotationProcessor 'com.github.ori-coval.Koala-Log:KoalaLoggingProcessor:1.5.7'
    implementation 'com.acmerobotics.dashboard:dashboard:0.6.0'


    // Unit tests. Run with: ./gradlew :TeamCode:testDebugUnitTest
    // or right-click a test class in Android Studio and choose Run.
    testImplementation 'junit:junit:4.13.2'
}
