# students/

One package per student working through `learning/` (see `learning/README.md`, *Per-student
package*): `students/<name>/` holds their lesson OpModes and a `PROGRESS.md`. Nothing here is
team code; `common/` and `teams/` never depend on it.
